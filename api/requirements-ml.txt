# AI / video pipeline — installed and wired in from Module 4 onward.
# Kept separate so the web service can run and be tested without ~4 GB of models.
faster-whisper>=1.1
opencv-python-headless>=4.10
mediapipe>=0.10
ultralytics>=8.3          # YOLOv11 weights + ByteTrack tracker integration
scenedetect[opencv]>=0.6.4
yt-dlp>=2025.1.1
numpy>=1.26
scipy>=1.14               # Savitzky-Golay crop-path smoothing
